# Repo files, not dotfiles
README.md

# Contains live tokens (fish universal variables)
.config/fish/fish_variables

# age key lives in 1Password (kubernetes vault), not in the repo
.config/sops/age/keys.txt

# Churny machine state
.ssh/known_hosts
.ssh/known_hosts.old

# Machine-local Claude Code settings
.claude/settings.local.json
.config/fish/.claude

# Embedded git repo (LazyVim starter clone)
.config/nvim/.git

# Managed by caelestia (symlinks)
.config/foot
.config/uwsm
