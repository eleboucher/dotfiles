$hypr = ~/.config/hypr
$hl = $hypr/hyprland

# Monitor layout (DP-2 left, DP-1 right)
monitor = , preferred, auto, 1
monitor = DP-2, preferred, 0x0, 1
monitor = DP-1, preferred, 2560x0, 1

# Workspaces pinned to monitors (1-5 left, 6-10 right)
workspace = 1, monitor:DP-2
workspace = 2, monitor:DP-2
workspace = 3, monitor:DP-2
workspace = 4, monitor:DP-2
workspace = 5, monitor:DP-2
workspace = 6, monitor:DP-1
workspace = 7, monitor:DP-1
workspace = 8, monitor:DP-1
workspace = 9, monitor:DP-1
workspace = 10, monitor:DP-1

# Kept from previous setup (env, input/NVIDIA cursor, vrr/misc, autostart, window rules)
source = $hl/env.conf
source = $hl/input.conf
source = $hl/misc.conf
source = $hl/execs.conf
source = $hl/rules.conf

# Ambxst
source = ~/.local/share/ambxst/hyprland.conf

# OVERRIDES
# Down here you can write or source anything that you want to override from Ambxst's settings.
# (Keybinds are managed in ~/.config/ambxst/binds.json and applied at runtime by axctl.)
