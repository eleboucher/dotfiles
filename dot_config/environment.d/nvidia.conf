# NVIDIA VA-API video decode. Previously in ~/.config/uwsm/env-sway, which is
# only read under uwsm - on the plain sway.desktop session these were unset and
# hardware video decode silently fell back to software.
LIBVA_DRIVER_NAME=nvidia
NVD_BACKEND=direct
MOZ_DISABLE_RDD_SANDBOX=1
